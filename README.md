# UWTutor

### Required Instillations
* python >= 3.6 / pip
* mongodb-community

### Server Setup
* `mongod --dbpath [db data location]`
* `make bootstrap`
* `python server/api.py`

### Client Setup
* `cd client && npm start`
